# Google Analytics 4 Measurement ID (safe to expose client-side).
# Cloudflare Web Analytics is NOT set here — the Cloudflare Pages integration
# injects its beacon automatically at the edge.
NEXT_PUBLIC_GA_ID=
